services:
  db:
    image: postgres:18.3-bookworm
    container_name: db
    restart: always
    shm_size: 128mb
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - ./postgresql_data:/var/lib/postgresql
    environment:
      POSTGRES_PASSWORD: 123456789
